# New top-level dependencies for this service (see CLAUDE.md Section 7 --
# named/approved in docs/build-log.md and the MCP-server plan). starlette
# comes in as mcp's own transitive requirement for the streamable-http
# transport; uvicorn is pinned explicitly since mcp_server.py imports and
# runs it directly rather than relying on mcp's optional [cli] extra.
mcp==2.0.0
uvicorn==0.52.4
# httpx2 arrives transitively via mcp (its HTTP/Streamable-HTTP backend --
# confirmed: `pip install mcp==2.0.0` resolves httpx2==2.12.0 + httpcore2).
# Pinned here because mcp_server.py imports it directly for its pooled
# client, so the version it runs against shouldn't move silently under a
# future mcp bump.
httpx2==2.12.0
