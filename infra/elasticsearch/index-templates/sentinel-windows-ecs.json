{
  "index_patterns": ["winlogbeat-ecs", "winlogbeat-ecs-*", "winlogbeat-*", "logs-windows.*"],
  "priority": 200,
  "template": {
    "settings": {
      "index.number_of_replicas": 0,
      "index.lifecycle.name": "sentinel-telemetry-retention",
      "index.lifecycle.rollover_alias": "winlogbeat-ecs"
    },
    "mappings": {
      "properties": {
        "@timestamp": { "type": "date" },
        "event": {
          "properties": {
            "code": { "type": "keyword" },
            "category": { "type": "keyword" }
          }
        },
        "host": {
          "properties": {
            "name": { "type": "keyword" }
          }
        },
        "process": {
          "properties": {
            "executable": { "type": "keyword" },
            "command_line": { "type": "keyword" }
          }
        }
      },
      "runtime": {
        "process.executable.caseless": {
          "type": "keyword",
          "script": {
            "source": "if (doc.containsKey(\"process.executable\") && doc[\"process.executable\"].size() != 0) { emit(doc[\"process.executable\"].value) }"
          }
        }
      }
    }
  }
}
